## Arkive AI Backend — Python Dependencies (v2)
## Install: pip install -r requirements.txt
## PyTorch CPU build — avoids 2GB CUDA download

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.128.8
uvicorn[standard]==0.34.3
python-multipart==0.0.22

# ── Database ──────────────────────────────────────────────────────────────────
pymongo[srv]==4.6.0
dnspython==2.4.2

# ── LLMs ─────────────────────────────────────────────────────────────────────
groq==1.0.0

# ── Embeddings + ML ───────────────────────────────────────────────────────────
fastembed==0.8.0

# ── LangChain (text splitting only — RAG now handled by custom retriever) ─────
langchain-text-splitters==1.1.1
langchain-core==1.2.14

# ── PDF Processing ────────────────────────────────────────────────────────────
PyMuPDF==1.27.1
pytesseract==0.3.10
pdf2image==1.17.0
Pillow>=10.3.0
python-docx>=1.1.2          # DOCX extraction for compliance check

# ── PDF Generation ────────────────────────────────────────────────────────────
reportlab==4.2.5                  # Pure Python PDF — always available
Jinja2==3.1.6                     # HTML templating for report
weasyprint==62.3; sys_platform != "win32"   # HTML→PDF (Linux/Mac prod)
# WeasyPrint requires GTK on Windows — use ReportLab fallback on Windows dev

# ── Auth ──────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
bcrypt==4.3.0                     # used directly in services/auth.py (passlib removed — incompatible with bcrypt 4.x)

# ── Caching ───────────────────────────────────────────────────────────────────
redis==5.2.1
numpy>=2.3.0                      # fastembed 0.8.0 requires numpy>=2.3.0

# ── BM25 Keyword Search (v2 RAG hybrid retrieval) ────────────────────────────
rank_bm25==0.2.2

# ── Config ───────────────────────────────────────────────────────────────────
python-dotenv==1.2.1

# ── Validation ───────────────────────────────────────────────────────────────
pydantic==2.12.5
email-validator==2.3.0

# ── HTTP ─────────────────────────────────────────────────────────────────────
httpx==0.28.1

# ── Billing (Phase 4.1) ───────────────────────────────────────────────────────
stripe==12.1.0

# ── Optional: Logging Shipping ────────────────────────────────────────────────
# logtail-python==0.3.1          # Uncomment when LOGTAIL_SOURCE_TOKEN is set
